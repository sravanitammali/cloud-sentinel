# ===========================================
# INSECURE Kubernetes Service
# CHECKOV WILL FLAG THIS!
# ===========================================
apiVersion: v1
kind: Service
metadata:
  name: insecure-service
spec:
  type: LoadBalancer # VULNERABILITY: Exposing to internet
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: insecure-web
  # VULNERABILITY: No network policies
